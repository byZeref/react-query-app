export interface User {
    id: string
    fullname: string
    username: string
    email: string
    image: string
    coutry: string
}

export interface IconProps {
  width?: string,
  height?: string,
  color?: string,
}